{
  "mcpServers": {
    "context-stream": {
      "type": "http",
      "url": "http://localhost:3000/mcp",
      "headers": {
        "Authorization": "Bearer ${MCS_TOKEN:-tok_agent}",
        "X-Agent-Name": "${MCS_AGENT_NAME:-agent-anon}"
      }
    }
  }
}
